// store/modules/user.js

const state = {
	// 模块1的状态
}

const getters = {
	// 模块1的getters
}

const mutations = {
	// 模块1的mutations
}

const actions = {
	// 模块1的actions
}

export default {
	namespaced: true,
	state,
	getters,
	mutations,
	actions
}